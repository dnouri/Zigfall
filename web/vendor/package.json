{
  "name": "zigfall-web-vendor",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild entry.mjs --bundle --format=esm --platform=browser --target=es2020 --legal-comments=eof --outfile=trystero-nostr.bundle.mjs && node patch-trystero-bundle.mjs",
    "test:pkt-limit": "node ../../tools/test_trystero_pkt_limit.mjs"
  },
  "dependencies": {
    "trystero": "0.25.2"
  },
  "devDependencies": {
    "esbuild": "0.25.5"
  }
}
